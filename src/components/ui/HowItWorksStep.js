function HowItWorksStep({ children }) {
  return (
    <li className="rounded-xl py-3 px-8 gap-1 shadow-sm grow flex flex-col items-start justify-center">
      {children}
    </li>
  );
}

export default HowItWorksStep;
